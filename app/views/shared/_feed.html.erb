<% if @feed_items.any? %>
<!-- @feed_itemsの中に情報があったら -->
  <ol class="microposts">
    <%= render @feed_items %>
    <!-- app/views/microposts/_micropost.html.erb -->
  </ol>
  <%= will_paginate @feed_items,
                    params: { controller: :static_pages, action: :home } %>
<% end %>
