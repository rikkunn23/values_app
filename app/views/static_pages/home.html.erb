

<% if logged_in? %>
  <div class="row">
    <aside class="col-md-4">
      <section class="user_info">
        <%= render 'shared/user_info' %>
      </section>

  <!-- follower-->
      <section class="stats">
        <%= render 'shared/stats' %>
      </section>

      <section class="micropost_form">
        <%= render 'shared/micropost_form' %>
      </section>
    </aside>

    <div class="col-md-8">
      <div class="row">
        <div class="col-md-4">
      <h3>Micropost Feed</h3>
    </div>
    <div class="search_form">
      <%= render 'shared/microposts_search_form' %>
    </div>
  </div>
      <%= render 'shared/feed' %>
    </div>
  </div>
<% else %>



<p class="video-p">The difference of values</p>
<div class="jumbotron  mb-0 wrap center jumbotron">

<div class="bg-video">
<%= video_tag("/videos/value-top.mp4", autoplay: :true, loop: :true, muted: :true, class: 'video-content') %>
</div>

<p class="video-p">Did you find an eternal theme?</p>
</div>

<%= link_to "新規アカウントを作成する", signup_path, class: "bttn" %>

<!-- <div class="center jumbotron"> -->
<!--
  <h2>
    This is the home page for the
    <a href="https://railstutorial.jp/">Ruby on Rails Tutorial</a>
    sample application.
  </h2> -->

  <!-- <= link_to "新規アカウントを作成する", signup_path, class: "btn btn-lg btn-primary" %>
</div>
  <= link_to image_tag("rails.svg", alt: "Rails logo", width: "200px"),
                      "https://rubyonrails.org/" %> -->
<% end %>
